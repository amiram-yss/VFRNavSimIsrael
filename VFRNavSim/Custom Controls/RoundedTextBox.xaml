<UserControl x:Class="VFRNavSim.Custom_Controls.RoundedTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VFRNavSim.Custom_Controls"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="80"
             SizeChanged="UserControl_SizeChanged">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border x:Name="brdFrame" Background="DimGray" Grid.RowSpan="2" Grid.ColumnSpan="3" CornerRadius="10"/>
        <TextBox x:Name="txt" Grid.Column="1" Height="20" Text="TextBox" Grid.Row="1" Background="Transparent" FontSize="12" BorderThickness="0" TextChanged="TextBox_TextChanged" KeyDown="Txt_KeyDown"/>

    </Grid>
</UserControl>
